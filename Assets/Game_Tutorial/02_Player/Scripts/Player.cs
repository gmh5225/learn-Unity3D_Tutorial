using UnityEngine;

// Configure every components + speed up music
public class Player : MonoBehaviour
{
	// Exercise 01:	Create a boolean variable "cameraControl" to enable/disable the control of the camera at Start of the application.
	// - if "cameraControl" is true : the character cannot strafe + CameraMovement script should be enabled
	// - if "cameraControl" is false : the character can strafe + CameraMovement script should be disabled
}